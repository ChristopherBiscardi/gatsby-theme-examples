import React, { Component } from "react";

export default class MyBio extends Component {
  render() {
    return (
      <div>
        The Bio.js component in the theme's src/components folder was replaced
        with this component
      </div>
    );
  }
}
